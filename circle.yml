dependencies: 
  pre:
    - sudo apt-get update; sudo apt-get install ssh-pass
    
deployment:
   staging:
      branch: master
      commands: 
        - scp -r grupo8/ grupo8@13.90.148.112:/var/www/grupo8/
